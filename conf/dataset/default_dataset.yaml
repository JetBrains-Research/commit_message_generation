testing: true

context_ratio:
generate_with_history: true
train_with_history: true

encoder_context_max_len: 512
decoder_context_max_len: 256
decoder_sep_tokens: "\n"

train_dataloader_conf:
  batch_size: 16
  num_workers: 4

val_dataloader_conf:
  batch_size: 16
  num_workers: 4

test_dataloader_conf:
  batch_size: 1
  num_workers: 1